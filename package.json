{
  "name": "bnz",
  "version": "0.1.0",
  "description": "Fetch Buganizer / issuetracker.google.com issues via authenticated headless Chromium.",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "bin": {
    "bnz": "./bnz.js"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
